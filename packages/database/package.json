{
  "name": "@repo/db",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "prisma": "^5.20.0"
  },
  "devDependencies": {
    "@prisma/client": "^5.20.0"
  }
}
